enum LoadingStatus { initial, loading, success, failed, failure }

enum MessageType { text, image, detection, loading, file, textExtraction }

enum DetectionType { label, text, face, object, logo, landmark ,imageProperty, web}
